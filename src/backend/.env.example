# Copy the values you need into shell env vars, your deploy dashboard, or the repo-root .env file.
# Local backend development works without DATABASE_URL and falls back to SQLite.

ADMIN_SECRET=demo-admin-secret
CORS_ALLOW_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
LLM_TIMEOUT_SEC=5
EAGER_LOAD_RAG=0
OLLAMA_URL=
GOOGLE_API_KEY=
GOOGLE_MODEL=gemma-3-27b-it
# DATABASE_URL=postgresql://raguser:ragpass@localhost:5432/history_rag
